{# src/Blogger/BlogBundle/Resources/views/Blog/entrante.html.twig #}
{% extends "BloggerBlogBundle::layout.html.twig" %}
{% block title %}Entrantes{% endblock %}

{% block body %}
	<h1>Entrantes</h1>
	{% for post in posts %}
        <article class="post">
            <header>
                <h2><a href="{{ path('blogger_blog_show', { 'id': post.id }) }}">{{ post.nombre }}</a></h2>
                <h3><a href="{{ path('blogger_blog_show', { 'id': post.id }) }}">{{ post.categoria }}</a></h3>
            </header>

            <div class="snippet">
                <p>{{ post.descripcion(200)|nl2br }}</p>
                <p class="continue"><a href="{{ path('blogger_blog_show', { 'id': post.id }) }}">Seguir leyendo...</a></p>
            </div>

            <footer class="meta">
                <p>Comentarios: {{ post.countComments }}</p>
            </footer>
        </article>
    {% else %}
        <p>No hay entradas en este blog</p>
    {% endfor %}
{% endblock %}